# Kai Salmon's Assignment
## Running
1. Install `requirements.txt`
2. Run `python main.py --help` to view options. To execute the example query, just use `python main.py`

## Running tests
Run `python -m unittest discover src/`